# 造船所パーツ置場管理アプリ 要件定義書

最終更新: 2026-04-24

## 1. 概要

造船所における鉄板加工工程〜組立工程間のパーツ置場を管理するiPhoneアプリ。
加工工程の担当者が搬出時にパーツを登録し、組立工程の担当者が使用時にステータス更新する。

## 2. 想定ユーザー

- **加工工程作業者**: パーツを置場に搬出した時点で登録
- **組立工程作業者**: パーツ使用時にステータスを「使用済み」に更新

## 3. 機能要件

### F1. パーツ登録

- カメラで印字文字をスキャン → OCRでテキスト抽出（Visionフレームワーク使用）
- OCR結果はユーザーが編集可能
- GPS位置情報を自動取得
- 登録日時を自動記録
- 初期ステータス = 「保管中」
- **同名既存時の挙動**: ダイアログで通知し、「上書き更新」or「別名保存」をユーザーが選択

### F2. パーツ検索

- 印字文字の**部分一致**検索
- カメラスキャン結果をそのまま検索クエリに流せる動線を用意
- 該当パーツの登録位置を地図で表示
- GPS誤差10m程度は許容（人間の目視探索で補完）

### F3. 位置情報更新（任意運用）

- 100m以上移動した場合のみユーザーが手動で位置更新
- 10m程度の置場内整理では更新不要

### F4. ステータス更新（使用済み化）

- カメラスキャンまたは検索で該当パーツを特定
- ステータスを「使用済み」に変更
- 使用日時を記録

### F5. 一覧表示・フィルタ

- ステータスでフィルタ可能
- **デフォルト表示は「保管中」のみ**
- 「使用済み」は履歴として残し、明示切替時のみ表示

## 4. データ項目

| 項目 | 内容 | 備考 |
|---|---|---|
| パーツ文字情報 | OCR結果（編集可） | 一意、重複時は選択制 |
| 緯度・経度 | 登録/更新時のGPS | |
| 登録日時 | 自動 | |
| 最終更新日時 | 自動 | |
| ステータス | 保管中 / 使用済み | |
| 使用日時 | 使用済み化時に記録 | |
| 登録者・更新者 | | 要検討（フェーズ2でユーザー識別が必要な場合） |

## 5. 非機能要件

- 屋外・手袋着用・騒音環境での操作性
- オフライン動作を前提（現場のWi-Fi不安定を想定）
- 複数人同時利用を前提としたデータ共有（フェーズ2）

## 6. フェーズ計画

- **フェーズ1**: ローカル保存のみ（Core Data または SwiftData）で動くものを作る
- **フェーズ2**: OneDrive連携（Microsoft Graph API経由での同期）に拡張

## 7. 技術スタック（想定）

- iOS / Swift / SwiftUI
- AVFoundation（カメラ）
- Vision（OCR: VNRecognizeTextRequest）
- Core Location（GPS）
- MapKit（地図表示）
- Core Data または SwiftData（ローカル保存）
- Microsoft Graph API（フェーズ2のOneDrive連携）

## 8. 配布・環境

- 対象端末: 会社支給iPhone（**CLOMO MDM**管理下）
- 配布方法: ABM（Apple Business Manager）カスタムAppを想定。代替案として一般配布 + CLOMO経由配信
- 検証環境: 個人端末、または会社端末のTestFlight
- 権限（カメラ / 位置情報 / ネットワーク）: MDMポリシー上問題なし

## 9. 未確定・要検討事項

- 画面構成・ユーザーフローの詳細
- データモデル（エンティティ定義）の確定
- 登録者・更新者の識別要否
- 使用済みデータの保存期間・アーカイブ方針
- OneDrive同期時のコンフリクト解消ルール
