# 決定事項ログ

本プロジェクトで合意した判断を時系列で記録する。
後から「なぜこう決めたか」を追えるように、決定内容と背景をセットで残す。

---

## 2026-04-24

### D-001 / OCR技術方針
- **決定**: iOS標準のVisionフレームワーク（VNRecognizeTextRequest）を使用
- **背景**: OCR技術自体の深掘りはしない。iPhoneの基本機能で最良の精度が出れば十分。
- **精度期待値**: 印刷文字 = 高、チョーク手書き = 中程度
- **補完策**: OCR結果はユーザーが修正可能

### D-002 / パーツ名重複時の扱い
- **決定**: ダイアログで通知 → ユーザーが「上書き」or「別名保存」を選択
- **背景**: パーツ名は原則一意だが、誤読や同名発生の可能性があるため

### D-003 / データ保存の段階計画
- **決定**: フェーズ1でローカル保存、フェーズ2でOneDrive連携
- **背景**: まず動くものを作り、バックエンドは後から整備する方針

### D-004 / 位置情報の精度要件
- **決定**: GPS誤差10m程度は許容。100m以上移動時のみ手動更新
- **背景**: 人間の目視探索で十分な範囲のため、厳密な追跡は不要

### D-005 / 検索方式
- **決定**: 部分一致検索。カメラスキャン結果をそのまま検索クエリに流せる動線も用意
- **背景**: 現場でキーボード入力より、カメラで再スキャンして照合する方が速い場面があるため

### D-006 / 一覧表示デフォルト
- **決定**: デフォルトは「保管中」のみ表示。使用済みは残すがフィルタで切替
- **背景**: 使用済みデータが蓄積しても日常業務が埋もれないようにする

### D-007 / 端末・配布方針
- **対象端末**: 会社支給iPhone（CLOMO MDM管理下）
- **配布**: ABMカスタムAppを第一候補、代替案として一般配布 + CLOMO経由配信
- **検証環境**: 個人端末、または会社端末のTestFlight
- **権限**: カメラ・位置情報・ネットワークともにMDMポリシー上問題なし

### D-008 / 開発管理方針
- **決定**: ローカルフォルダ `~/Documents/ShipyardPartsApp/` をプロジェクトの単一拠点とする
- **背景**: 仕様書・決定事項・将来的なSwiftコードまで1か所に集約することで、セッション/端末をまたいでも継続しやすくする

### D-009 / 画面構成
- **決定**: 5画面構成（ホーム / カメラ+OCR / 詳細 / 検索 / 検索結果一覧）
- **詳細画面は3モード兼用**: 登録モード / 閲覧モード（ReadOnly）/ 編集モード
- **詳細画面は2タブ**: 情報 / 地図
- **背景**: 詳細表示と登録入力を1画面に統合することで画面数を抑え、作業者の学習コストを下げる

### D-010 / 破壊的アクションには確認ダイアログ
- **決定**: 「使用済みにする」「位置情報を更新する」「置場に戻す」は全て確認ダイアログを挟む
- **位置情報更新の文言例**: 「この端末の現在地が所在地になりますがよろしいですか？」
- **背景**: 誤操作防止。現場作業中は手袋やボタンの誤タップが起こりやすい

### D-011 / 詳細画面に「編集モード」を設ける
- **決定**: 閲覧モードから「編集」ボタンで編集モードに切替可能とする
- **背景**: OCR誤認識を後から修正する運用ニーズがある

### D-012 / ステータス別の詳細画面ボタン表示
- **保管中**: 編集 / 使用済みにする / 位置情報を更新する
- **使用済み**: 置場に戻す のみ（編集・位置更新は非表示）
- **背景**: 「置場に戻す」は使用済み誤登録からの復帰用。使用済みになった後の項目修正・位置更新は運用上不要

### D-013 / 「置場に戻す」時の位置情報の扱い
- **決定**: 既存の位置情報を維持する（更新しない）
- **背景**: 「置場に戻す」は誤操作訂正の想定。位置は登録当時のまま残すのが自然

### D-014 / 登録完了通知
- **決定**: 保存成功時に画面下部へ「登録しました」等のトーストを短時間（1〜2秒）表示
- **背景**: 手袋・騒音環境下で「保存できた」が視覚的に分かる必要がある

### D-015 / 検索結果の件数表示
- **決定**: 結果一覧のヘッダに「○件ヒット」を表示。0件時は「該当するパーツはありません」を明示
- **背景**: 検索精度を把握できるようにし、該当なし時の戸惑いを防ぐ

### D-016 / 編集モード離脱時の警告
- **決定**: 変更がある状態で離脱しようとしたら「変更を破棄しますか？」の確認ダイアログを表示
- **背景**: 誤操作による変更内容の消失を防ぐ

### D-017 / 写真は1レコードあたり最大2枚
- **決定**: Part エンティティは最大2枚の写真を保持できる
- **背景**: 全体像と印字のクローズアップで十分。容量増の抑制

### D-018 / 登録者・更新者を保持する
- **決定**: Part に `createdBy` / `updatedBy` を持つ。PartEvent にも `actorId` / `actorName` を記録
- **フェーズ1**: 初回起動時にユーザー名を入力させ、端末IDとセットで使う
- **フェーズ2**: Microsoftアカウントに切替
- **背景**: 誰がいつ何をしたかを追えるようにするため

### D-019 / 変更履歴は監査証跡として持つ
- **決定**: 別エンティティ PartEvent として保持。**append-only**（更新・削除禁止）
- **記録内容**: イベント種別、変更前後の値、実行者、発生時刻、発生時の端末位置
- **背景**: 監査要件に耐える粒度にするため。単なるログではなくデータとして意味を持たせる

### D-020 / 備考欄 notes を持つ
- **決定**: Part に `notes: String?` を持つ（自由記述）
- **背景**: 現場で気づいたことを残せるようにする

### D-021 / Actor識別方式（フェーズ1）
- **決定**: 端末一意ID（identifierForVendor）と、初回起動時に入力するユーザー名のペアで識別
- **背景**: フェーズ1はシンプルに。フェーズ2でMicrosoftアカウントに切替予定

### D-022 / 写真の圧縮設定
- **決定**: JPEG品質80%、長辺1600pxへリサイズして保存
- **背景**: OCR用途としての画質は十分で、容量増を抑えられる

### D-023 / OCR元画像の自動保存
- **決定**: 登録時のOCR対象画像を自動で1枚目の写真として保存。2枚目は任意で追加撮影
- **背景**: 証跡として元画像があると監査で強い。撮影操作は最小限に抑えたい

### D-024 / 削除は論理削除のみ
- **決定**: 物理削除は行わない。`isDeleted` フラグで非表示化し、PartEventは残す
- **背景**: 監査要件上、物理削除はリスクが高い。誤登録時の対応は論理削除で十分

### D-025 / 備考欄の最大文字数
- **決定**: 500文字
- **背景**: 現場メモとしては十分。長文は想定しない

### D-026 / 最低対応iOSバージョン
- **決定**: iOS 17以上
- **背景**: SwiftData を使えるため。対象端末（iPhone SE第3世代・iPhone 16e）は両方とも iOS 17以上対応

### D-027 / UI言語
- **決定**: 日本語のみ
- **背景**: 全ユーザーが日本語話者。多言語対応は不要

### D-028 / OCR認識言語
- **決定**: 日本語と英数字の複数言語指定
- **背景**: パーツ番号は英数字中心だが、手書きで日本語混じりがあり得る

### D-029 / 地図プロバイダ
- **決定**: MapKit（Apple Maps）
- **背景**: iOS標準で追加キー不要、コスト管理不要

### D-030 / オフライン動作
- **決定**: 全機能をオフラインで動作可能とする
- **背景**: 造船所の置場は電波が不安定な場所が多い

### D-031 / データ暗号化
- **決定**: iOS Data Protection に委譲。`NSFileProtectionCompleteUntilFirstUserAuthentication` を指定
- **背景**: 独自暗号化は複雑化の元。OSの仕組みを使うのが堅い

### D-032 / iCloudバックアップ対象外
- **決定**: ローカルDB・写真ともに `.noBackup` 属性を付与し、iCloudバックアップ対象から外す
- **背景**: 業務データが個人iCloudに同期されるリスクを回避

### D-033 / タップターゲットサイズ
- **決定**: 主要ボタンは最低48×48ptのタップ領域を確保
- **背景**: 手袋・屋外・騒音環境での誤タップを抑える

### D-034 / UI設計の基準端末
- **決定**: iPhone SE 第3世代（4.7"）を基準に画面レイアウトを設計
- **背景**: 対象端末の中で最も狭いため。ここで崩れなければ大きい画面でも問題ない

### D-035 / Xcodeプロジェクト基本情報
- **Xcodeプロジェクト名**: `ImazoStorage`（ASCII）
- **アプリ表示名**: `IMAZO置場管理`（日本語）
- **Bundle ID**: `dev.matsuokaryoma.ImazoStorage`
- **背景**: 個人アカウントで開始。ASCIIと日本語を使い分けてビルド・Git管理のトラブルを避ける

### D-036 / 開発アカウントは個人で開始
- **決定**: フェーズ1は個人Apple IDで開発（必要になれば有料 Apple Developer Program 登録）
- 会社展開時は**新Bundle IDで会社アカウントに新規登録**
- **背景**: まずは個人で試作し、うまく行ったら会社展開する方針

### D-037 / Gitリポジトリ構成
- **決定**: `~/Documents/ShipyardPartsApp/` をGitリポジトリとし、仕様書とXcodeプロジェクトを同一リポジトリで管理
- リモートは当面なし（ローカルのみ）
- **背景**: 単一の場所に集約して管理しやすくする

---

## 2026-04-25

### D-038 / Bundle ID は大文字Iの `ImazoStorage` を採用
- **決定**: Bundle ID を `dev.matsuokaryoma.ImazoStorage`（大文字I）とする
- **背景**: Xcodeのプロジェクト作成時に Product Name から自動生成された値が `dev.matsuokaryoma.ImazoStorage` で、Apple Free Provisioning に既に登録済みだった。小文字に変更しようとしたところ "cannot be registered to your development team because it is not available" エラーが発生（Apple側はBundle IDを大文字小文字区別せず同一とみなすため）。機能的には大文字小文字どちらでも問題なく、プロジェクト名と一致させるほうが自然なため、大文字版を正とする。
- **影響**: CLAUDE.md / decisions.md (D-035) の Bundle ID 記述を更新済み

### D-039 / 対象端末に iPad を追加
- **決定**: Supported Destinations に iPhone と iPad を含める（Mac / Apple Vision は除外）
- **UI設計基準は iPhone SE 第3世代のまま**（D-034 を維持）
- **背景**: オーナーが iPad でも操作したいため。ただしフェーズ1では iPad 専用最適化（サイドバー等）は行わず、「大きい画面のスマホ」風レイアウトで動作させる
- **影響**: requirements.md / non_functional.md に対象端末の記述があれば更新が必要
