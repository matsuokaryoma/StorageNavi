# 画面構成・ユーザーフロー

最終更新: 2026-04-24

## 画面一覧

| # | 画面 | 役割 |
|---|---|---|
| 1 | ホーム画面 | 登録 / 検索 の分岐 |
| 2 | カメラ+OCR画面 | 登録時のみ通過 |
| 3 | 詳細画面 | 登録・閲覧・編集を1画面で兼ねる（モード切替） |
| 4 | 検索画面 | 検索条件入力。カメラでOCR検索も可能 |
| 5 | 検索結果一覧 | ヒットしたパーツのリスト |

---

## 詳細画面のモード

| モード | 項目活性 | 到達経路 |
|---|---|---|
| 登録モード | 編集可 | ホーム → カメラ+OCR後 |
| 閲覧モード | ReadOnly | 検索結果タップ（初期状態） |
| 編集モード | 編集可 | 閲覧モードから「編集」ボタン |

## 詳細画面のタブ

- **情報タブ**: 項目表示（モードに応じて活性/非活性）
- **地図タブ**: 登録位置を地図表示（常にReadOnly）

## 詳細画面のアクションボタン（ステータス別）

### ステータス = 保管中

| ボタン | 挙動 | 確認ダイアログ |
|---|---|---|
| 編集 | 閲覧モード → 編集モードへ切替 | 不要 |
| 使用済みにする | ステータスを「使用済み」に変更 | **必要** |
| 位置情報を更新する | 現在地GPSで所在地を上書き | **必要**（文言例：「この端末の現在地が所在地になりますがよろしいですか？」）|

### ステータス = 使用済み

| ボタン | 挙動 | 確認ダイアログ |
|---|---|---|
| 置場に戻す | ステータスを「保管中」に戻す | **必要** |

※ 使用済み時は「編集」「位置情報更新」ボタンは **表示しない**

---

## ユーザーフロー

### A. 登録フロー（加工のおじちゃん）
1. ホーム → 「登録」
2. カメラ起動 → 被写体にかざす
3. OCR実行 → 文字抽出 → GPS自動取得
4. 詳細画面（登録モード）に抽出結果を表示
5. 目視チェック・必要なら修正
6. 「保存」→ 新規レコード作成 → ホームへ戻る
   - 同名が既存の場合は「上書き」or「別名保存」を選択（D-002）

### B. 検索フロー（両方のおじちゃん共通）
1. ホーム → 「データを探す」
2. 検索画面で条件入力 or カメラ検索（OCR結果を検索クエリへ）
3. 検索結果一覧表示
4. パーツをタップ → 詳細画面（閲覧モード）
5. 情報タブ / 地図タブを切替

### C. 使用済み化フロー（組立のおじちゃん）
1. フローBで詳細画面を開く
2. 「使用済みにする」タップ → 確認ダイアログ → OKで保存
3. ステータスが「使用済み」に更新

### D. 位置情報更新フロー（加工のおじちゃん）
1. フローBで詳細画面を開く
2. 「位置情報を更新する」タップ → 確認ダイアログ → OKで保存
3. 現在地GPSで所在地が上書きされる

### E. 訂正フロー
1. フローBで詳細画面を開く
2. 「編集」タップ → 編集モードへ
3. 項目修正 → 「保存」で既存レコードを更新

### F. 使用済み取消フロー
1. 検索時にステータスフィルタを「使用済み」に切替
2. 対象パーツをタップ → 詳細画面（閲覧モード）
3. 「置場に戻す」タップ → 確認ダイアログ → OKで保存
4. ステータスが「保管中」に戻る

---

## 補足仕様（確定済み）

- **登録完了通知**: 保存成功時に画面下部へ「登録しました」等のトーストを短時間表示（D-014）
- **検索結果件数表示**: 結果一覧のヘッダに「○件ヒット」を表示。0件時は「該当するパーツはありません」を明示（D-015）
- **編集モード離脱警告**: 変更がある状態で画面を閉じようとしたら「変更を破棄しますか？」の確認ダイアログを表示（D-016）

## 未確定事項

（現在なし — 画面仕様はFIX）
